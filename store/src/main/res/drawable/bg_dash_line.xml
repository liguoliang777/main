<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
       android:shape="line">
    <!--显示一条虚线，width为线条的高度，注意：他的值不能比xml中设置的小
    dashWidth为破折线的宽度，
    dashGap为破折线之间的空隙的宽度，
    当dashGap=0时，就是实线-->
    <stroke
        android:width="@dimen/dm001"
        android:color="@color/d9d9d9"
        android:dashGap="@dimen/dm009"
        android:dashWidth="@dimen/dm010"/>
</shape>